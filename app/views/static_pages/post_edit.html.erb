<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>SmileTalk 投稿管理</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root {
      --main-color: #b79fcb;
      --main-color-dark: #a487c0;
      --soft-color: #f2edf7;
      --soft-color-dark: #dbcde9;
    }
    body { margin: 0; font-family: sans-serif; background: #faf7fc; }
    header { background-color: var(--main-color); padding: 15px 0; text-align: center; }
    header .site-title { font-family: 'Pacifico', cursive; font-size: 2rem; color: #fff; margin: 0; text-shadow: 0 3px 6px rgba(0,0,0,0.1); }
    .page-wrapper { max-width: 850px; margin: 40px auto 80px auto; }
    .post-list-link { color: #8b6fa4; font-weight: bold; }
    .post-list-link:hover { text-decoration: none; color: #6c4e89; }
    .custom-card { border-radius: 12px; border: 1px solid #e8e2f0; box-shadow: 0 3px 6px rgba(183,159,203,0.15); padding: 20px; background: #fff; margin-bottom: 20px; }
    .btn-custom { background-color: var(--main-color); border: none; color: #fff; padding: 10px 16px; border-radius: 6px; cursor: pointer; transition: 0.2s; font-family: 'Pacifico', cursive; }
    .btn-custom:hover { background-color: var(--main-color-dark); }
    .btn-delete { background-color: #e79ac3; border: none; color: #fff; padding: 10px 16px; border-radius: 6px; cursor: pointer; transition: 0.2s; font-family: 'Pacifico', cursive; }
    .btn-delete:hover { background-color: #d881b2; }
    footer { margin-top: 40px; background-color: var(--soft-color); padding: 10px 0; text-align: center; color: #7a6a8c; font-size: 0.9rem; }
    form .form-group { text-align: left; margin-bottom: 15px; }
    label { font-weight: bold; color: #6f5d80; }
    .form-control, textarea { border-radius: 6px; border: 1px solid #ccc; }
  </style>
</head>
<body>
  <header>
    <h1 class="site-title">SmileTalk</h1>
  </header>

  <div class="page-wrapper">
    <div class="text-center mb-4">
      <a href="/static_pages/thread_index" class="post-list-link">投稿一覧を見る</a>
    </div>

    <!-- 投稿の修正 -->
    <div class="custom-card">
      <div class="text-center">
        <h2 class="h5 mb-3 fw-bold" style="color:#6c4e89;">投稿の修正</h2>
        <%= form_with url: static_pages_update_post_path, method: :post, class: "mx-auto", style: "max-width:500px;" do %>
          <div class="form-group">
            <label>投稿ID</label>
            <input type="text" name="post_id" class="form-control">
          </div>
          <div class="form-group">
            <label>新しい内容</label>
            <textarea name="content" class="form-control" rows="3"></textarea>
          </div>
          <button type="submit" class="btn btn-custom px-4 py-2">修正する</button>
        <% end %>
      </div>
    </div>

    <!-- 投稿の削除 -->
    <div class="custom-card">
      <div class="text-center">
        <h2 class="h5 mb-3 fw-bold" style="color:#6c4e89;">投稿の削除</h2>
        <%= form_with url: static_pages_delete_post_path,
                      method: :post,
                      class: "mx-auto",
                      style: "max-width:500px;",
                      data: { turbo_confirm: "本当に削除しますか？" } do %>

          <div class="form-group">
            <label>投稿ID</label>
            <input type="text" name="post_id" class="form-control">
          </div>
          <button type="submit" class="btn btn-delete px-4 py-2">削除する</button>
        <% end %>
      </div>
    </div>
  </div>

  <footer>
    SmileTalk © 2025
  </footer>

  
  <script>
    function confirmDelete() {
      return confirm("本当に削除しますか？");
    }
  </script>

</body>
</html>
