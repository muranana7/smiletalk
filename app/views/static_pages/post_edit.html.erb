<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <title>SmileTalk 投稿管理</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root {
      --main-color: #b79fcb;
      --main-color-dark: #a487c0;
      --soft-color: #f2edf7;
      --soft-color-dark: #dbcde9;
    }
    body { margin: 0; font-family: sans-serif; background: #faf7fc; }
    header { background-color: var(--main-color); padding: 15px 0; text-align: center; }
    header .site-title { font-family: 'Pacifico', cursive; font-size: 2rem; color: #fff; margin: 0; }
    .page-wrapper { max-width: 850px; margin: 40px auto 80px auto; }
    .custom-card { border-radius: 12px; border: 1px solid #e8e2f0; padding: 20px; background: #fff; margin-bottom: 20px; }
    .btn-custom { background-color: var(--main-color); color: #fff; }
    .btn-delete { background-color: #e79ac3; color: #fff; }
  </style>
</head>

<body>
<header>
  <h1 class="site-title">SmileTalk</h1>
</header>

<div class="page-wrapper">

  <!-- 投稿の修正 -->
  <div class="custom-card">
    <h2>投稿の修正</h2>
    <%= form_with url: static_pages_update_post_path, method: :post do %>
      <div>
        <label>投稿ID</label>
        <%= text_field_tag :post_id %>
      </div>
      <div>
        <label>新しい内容</label>
        <%= text_area_tag :content %>
      </div>
      <%= submit_tag "修正する", class: "btn btn-custom" %>
    <% end %>
  </div>

  <!-- 投稿の削除 -->
  <div class="custom-card">
    <h2>投稿の削除</h2>
    <%= form_with url: static_pages_delete_post_path, method: :post,
          data: { turbo_confirm: "本当に削除しますか？" } do %>
      <div>
        <label>投稿ID</label>
        <%= text_field_tag :post_id %>
      </div>
      <%= submit_tag "削除する", class: "btn btn-delete" %>
    <% end %>
  </div>

</div>

</body>
</html>
