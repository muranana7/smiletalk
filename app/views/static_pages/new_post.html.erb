<!DOCTYPE html>
<html lang="ja">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>新規投稿ページ</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <style>
    :root {
      --main-color: #b79fcb;
      --main-color-dark: #a487c0;
      --secondary-bg: #faf7fc;
      --text-color: #333;
    }

    body {
      font-family: "Inter", sans-serif;
      background-color: var(--secondary-bg);
      color: var(--text-color);
    }

    .custom-header {
      background-color: var(--main-color);
      padding: 15px 0;
      text-align: center;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -2px rgba(0, 0, 0, 0.1);
    }
    .custom-header .site-title {
      font-size: 2rem;
      color: #fff;
      margin: 0;
      font-weight: bold;
      text-shadow: 0 3px 6px rgba(0, 0, 0, 0.1);
    }

    .bg-main-color { background-color: var(--main-color); }
    .hover\:bg-main-color-dark:hover { background-color: var(--main-color-dark); }

  </style>
</head>

<body class="min-h-screen p-4 sm:p-8 flex justify-center items-start">

  <div class="w-full max-w-2xl bg-white shadow-xl rounded-xl p-6 sm:p-10 border border-gray-100">
    <h1 class="text-3xl font-bold text-gray-800 mb-6 border-b pb-2">新規投稿作成</h1>

    <% if flash[:notice] %>
      <div class="mb-4 p-4 bg-green-100 text-green-700 rounded-lg border border-green-300">
        <%= flash[:notice] %>
      </div>
    <% elsif flash[:alert] %>
      <div class="mb-4 p-4 bg-red-100 text-red-700 rounded-lg border border-red-300">
        <%= flash[:alert] %>
      </div>
    <% end %>

    <%= form_with url: static_pages_create_post_path, method: :post, local: true, html: { class: "space-y-6" } do |f| %>
      <div>
        <%= f.label :title, "タイトル", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.text_field :title, required: true, class: "mt-1 block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-base", placeholder: "タイトルを入力してください..." %>
      </div>
      
      <div>
        <%= f.label :content, "本文", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.text_area :content, rows: 8, required: true, class: "mt-1 block w-full px-4 py-3 border border-gray-300 rounded-lg shadow-sm focus:ring-indigo-500 focus:border-indigo-500 text-base", placeholder: "本文を自由に入力してください..." %>
      </div>

       <div>
        <%= f.label :image, "写真添付", class: "block text-sm font-medium text-gray-700 mb-2" %>
        <%= f.file_field :image, accept: "image/*", class: "mt-1 block w-full text-sm text-gray-700 border border-gray-300 rounded-lg cursor-pointer focus:outline-none" %>
        <p class="mt-1 text-xs text-gray-500">対応形式: JPG, JPEG, PNG, GIF, WebP</p>    
      </div>

      <div class="pt-4">
        <%= f.submit "投稿する", class: "w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-md text-lg font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-150 ease-in-out disabled:opacity-50" %>
      </div>
    <% end %>
  </div>

</body>
</html>